apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: gitea
  namespace: gitea
spec:
  endpoints:
    - dnsName: gitea.malef.lan
      recordType: A
      targets:
        - 192.168.1.15